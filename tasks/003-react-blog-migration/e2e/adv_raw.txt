# Adversarial raw — 2026-08-31, прогон по заданию «Миграция блога md_articles на React»

Среда: backend uvicorn на :8000 (PID 1676418, остался работать), frontend/dist собран.
Бэкапы: /tmp/adv_articles.yaml.bak(2), /tmp/adv_content_bak/, /tmp/adv_content_art.ls, /tmp/adv_pics.ls.
Итог состояния: articles.yaml и content_art/ восстановлены (diff чист), static/profile_pics/ без изменений.

## Ключевые сырые выводы

### CSRF
- POST /api/blog/logout БЕЗ X-CSRF-Token → {"message":"You have been logged out","category":"success"} HTTP=200
- register/login/meta/add_all/account без/с неверным CSRF → {"detail":"CSRF token mismatch"} HTTP=403 (корректно)
- GET /api/blog/csrf повторно залогиненной сессией → cookie session меняется на новый анонимный (user_id исчезает);
  последующий add_all/meta тем же jar → {"detail":"Authentication required"} [403]
- POST /api/blog/account с корректным X-CSRF-Token, Content-Type: application/json → {"detail":"CSRF token mismatch"} [403]
  (только form-поле csrf_token принимается; заголовок игнорируется)
- POST /api/blog/account multipart с корректным полем, но .txt замаскирован под .png → Internal Server Error [500]
  Трейсбек: api_blog.py:381 → _save_picture api_blog.py:206 → PIL.UnidentifiedImageError: cannot identify image file

### Регистрация/логин
- дубликат username: 422 {"errors":{"username":["That username is taken..."]}}
- дубликат email: 422 {"errors":{"email":["That email is taken..."]}}
- пустое тело {}: 422 все поля required; невалидный JSON: 422 {"errors":{"body":["Invalid JSON body."]}}
- username 10000 символов: 422 между 2 и 20; пробельный username: 422 required
- unicode username "юникод😀用户": 200 создан; эмодзи в username через account тоже 200
- login неверный пароль / неизвестный email: 401 {"message":"Login Unsuccessful..."}
- login с " adv1@t.io " (пробелы): 200 УСПЕХ; login UPPER CASE: 401
- remember:"yes" (строка вместо bool): 200, cookie session получили Max-Age 1789402886 (remember-флаг сработал)

### art_manage
- meta "../../etc/passwd", "/etc/passwd", несуществующий файл, пустой file_name → 422 "Недопустимое имя файла"
- meta по уже зарегистрированному файлу (FastAPI_CodeReview.docx.md) → 200 "Обновлена запись" (редактирование
  существующих записей, не только новых)
- add_all нового файла → 200 "Добавлено файлов: 1", запись создана с author:'' lang:'' title: file stem
- add_all повторный → 200 "Нет новых файлов для добавления" (идемпотентен)
- art_manage GET/POST анонимом → 403 {"detail":"Authentication required"}

### SPA fallback / пути
- GET / (SPA root): 200, 7450 байт index.html
- GET /api (без слэша) → 200 index.html (ДОЛЖЕН быть 404 JSON по контракту фазы 7)
- GET /api/ (со слэшем), /api/blog/nonexistent → 404 {"detail":"Not Found"} (корректно)
- HEAD /api/blog/articles → 404 (стабильно, raw-socket подтверждён); HEAD на catch-all → 200 (корректно для FileResponse)
- POST/PUT/DELETE/PATCH на catch-all → 405
- /static/../md_articles/articles.yaml, /assets/../main.py, /assets/%2e%2e/main.py → 404 (traversal заблокирован)
- //api/blog/csrf → 200 (доступ по двойному слэшу), //art_home → 200 index.html

### articles/{id}
- -1, 0, 99999999999999999999 → 404; "abc" → 422; "😀" → 400 {"detail":"Article not found"}
- валидный 1787932544 → 200, content содержит отрендеренный HTML

### account
- GET анонимом → 403; GET с мусорной/поддельной cookie → user:null / 403 (подмена cookie не работает)
- пустой username → 422; unicode username → 200 500 на не-изображение — см. выше
- JSON body на multipart-роут → 403 CSRF mismatch (не краш)### qa triage repro, 2026-08-31T16:29:43Z
== prep: GET /api/blog/csrf (fresh jar) ==
csrf resp: {"csrf_token":"b315cbdb925412d2707c7b652e07f0a016b072d1b37e8fb9622a1bdb148b3cdc"}
extracted token: b315cbdb925412d2707c7b652e07f0a016b072d1b37e8fb9622a1bdb148b3cdc

== DEF-001: register+login, then POST /api/blog/logout WITHOUT CSRF header ==
-- register:
{"errors":{"confirm_password":["This field is required."]}}
http=422
-- login:
{"message":"Login Unsuccessful. Please check email and password","category":"danger"}
http=401
-- current_user check:
{"user":null}
http=200
-- POST /api/blog/logout (no CSRF header, no field):
{"message":"You have been logged out","category":"success"}
http=200


== DEF-001 retry: register with confirm_password ==
-- register:
{"message":"Your account has been created! You are now able to log in","category":"success"}
http=200
-- login:
{"message":"You are now logged in","category":"success","user":{"id":10,"username":"qa_def","email":"qa_def@example.com","image_file":"/static/profile_pics/default.jpg"}}
http=200
-- current_user:
{"user":{"id":10,"username":"qa_def","email":"qa_def@example.com","image_file":"/static/profile_pics/default.jpg"}}
http=200
-- POST /api/blog/logout WITHOUT CSRF:
{"message":"You have been logged out","category":"success"}
http=200
-- current_user after:
{"user":null}
http=200

== DEF-002: multipart POST /api/blog/account, csrf_token + fake .png (text content) ==
-- login again (fresh session):
{"message":"You are now logged in","category":"success","user":{"id":10,"username":"qa_def","email":"qa_def@example.com","image_file":"/static/profile_pics/default.jpg"}}
http=200
-- POST /api/blog/account (multipart, masked png):
Internal Server Error
http=500
-- server log tail (UnidentifiedImageError?):
1102:  File "/home/max/0_0_26_new_one/my-fastapi-one/fastapi-application/md_articles/api_blog.py", line 206, in _save_picture
1106:    raise UnidentifiedImageError(msg)
1107:PIL.UnidentifiedImageError: cannot identify image file <_io.BytesIO object at 0x77e44801a390>

== DEF-003: GET /api (no slash, --path-as-is) ==
-- GET /api:
http=200 content_type=text/html; charset=utf-8 size=7450
<!doctype html>
<html lang="ru" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" conte
-- GET /api/ (slash):
http=404 content_type=application/json body={"detail":"Not Found"}
-- GET /api/blog/nonexistent:
{"detail":"Not Found"} http=404
