# Переделка «Управления»: список + одна форма + фильтры + поиск

Прямое задание пользователя без спеки. Контракт передан промптом прогона frontend-dev.

## Подтверждённые решения (от пользователя, 2026-09-01)

- Вместо таблицы с формой в каждой строке — список названий статей без кнопок.
- Клик по статье — выбор для редактирования; один набор полей (автор/язык/заголовок)
  и кнопка «Принять».
- Фильтр «Без автора»: NoName = «без автора» (пустая строка — тоже).
- Фильтр «Без языка»: пустой lang.
- Поиск по названию статьи как фильтр (подстрока, без учёта регистра).

## Реализация

- `frontend/src/pages/ArtManagePage.tsx` — переделан: блок фильтров (поиск +
  чекбоксы «Без автора»/«Без языка» + счётчик «Найдено: N»), кликабельный список
  `.registry-item` (title крупно, приглушённо «автор · язык», активный — градиентом),
  пагинация готовым `<Pagination>` (5/10/20, сброс page=1 при смене фильтров), пустое
  состояние «Ничего не найдено по фильтрам», панель `.art-edit-panel` с формой выбранной
  записи; секции «Новые файлы»/«Записи без файла»/«Добавить все новые файлы»/yaml_error
  сохранены.
- `frontend/src/components/ArtManageForms.tsx` — удалён `ArtEditForm` (форма в строке),
  добавлен `ArtEditSelectedForm` (одна форма выбранной записи, кнопка «Принять»,
  422-ошибки по полям); `ArtAddForm` не тронут.
- `frontend/src/index.css` — стили `.registry-*`, `.art-edit-panel` по образцу `.menu-item`.
- Бэкенд/API не менялись (updateMeta уже умеет всё нужное).

---

# Отчёт о выполнении

- Дата закрытия: 2026-09-01
- Коммит: не коммитилось (по явному правилу пользователя после инцидента с коммитом)

## Итог

Страница «Управление реестром» переделана по контракту: список названий без кнопок,
выбор кликом, одна форма редактирования с «Принять», фильтры «без автора» (NoName) и
«без языка», поиск по названию. Сборка зелёная.

## Критерии успеха (задание без спеки — проверка оркестратором)

| # | Проверка | Результат | Доказательство |
|---|---|---|---|
| 1 | `npm run build` без ошибок | PASS | dev/artmanage-rework_raw.txt + контроль оркестратора (✓ built in 1.62s) |
| 2 | Фильтры по контракту (NoName/пустой автор; пустой lang; поиск по title+file_name, ci) | PASS | ревью диффа: isNoAuthor/isNoLang/matchesSearch |
| 3 | Старая форма в строке удалена | PASS | grep: ArtEditForm отсутствует в src/ |
| 4 | Дифф в рамках (3 файла) | PASS | git diff --stat: ArtManagePage + ArtManageForms + index.css |

## Дефекты

Не найдены — DEFECTS.md не создавался.

## Adversarial-прогон

Не выполнялся (задание без спеки, по образцу предыдущих).

## Участники

- frontend-dev: переделка страницы и форм (dev/artmanage-rework_progress.md, dev/artmanage-rework_raw.txt)
- оркестратор: контракт, ревью диффа, контрольная сборка, архивирование
